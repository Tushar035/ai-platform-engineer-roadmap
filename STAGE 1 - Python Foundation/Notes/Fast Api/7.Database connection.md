---

# 🗄️ FastAPI PHASE 5 — Database Integration (SQLAlchemy, CRUD, Sessions & Migrations)

Database integration is the backbone of any real-world application.

In this phase, we will cover:

- Connecting FastAPI with SQL Databases
- CRUD Operations with SQLAlchemy
- Database Sessions & Dependency Injection
- Migrations & Production DB Setup

---

# 📌 1. Connecting FastAPI with SQL Databases

## 🧠 Why Database Integration?

APIs are useful only when they can:
- Store data
- Retrieve data
- Update/Delete data

---

## 📦 Install Dependencies

```bash
pip install sqlalchemy psycopg2-binary alembic
```
👉 Optional (for async later):

pip install asyncpg


---

🧱 Project Structure (Recommended)

app/
 ├── main.py
 ├── database.py
 ├── models/
 ├── schemas/
 ├── crud/
 └── routers/


---

🔌 Database Connection (database.py)

from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, declarative_base

DATABASE_URL = "postgresql://user:password@localhost/dbname"

engine = create_engine(DATABASE_URL)

SessionLocal = sessionmaker(bind=engine, autoflush=False, autocommit=False)

Base = declarative_base()


---

💡 Explanation

engine → connects to database

SessionLocal → creates DB sessions

Base → base class for models



---

🧱 Create Model (models/user.py)

from sqlalchemy import Column, Integer, String
from app.database import Base

class User(Base):
    __tablename__ = "users"

    id = Column(Integer, primary_key=True, index=True)
    name = Column(String)
    email = Column(String, unique=True, index=True)


---

🔄 Create Tables

Base.metadata.create_all(bind=engine)


---

📌 2. CRUD Operations with SQLAlchemy


---

📥 Create (Insert)

def create_user(db, name: str, email: str):
    user = User(name=name, email=email)
    db.add(user)
    db.commit()
    db.refresh(user)
    return user


---

📤 Read (Select)

def get_user(db, user_id: int):
    return db.query(User).filter(User.id == user_id).first()


---

🔄 Update

def update_user(db, user_id: int, name: str):
    user = db.query(User).filter(User.id == user_id).first()
    user.name = name
    db.commit()
    return user


---

❌ Delete

def delete_user(db, user_id: int):
    user = db.query(User).filter(User.id == user_id).first()
    db.delete(user)
    db.commit()


---

💡 Explanation

add() → stage object

commit() → save to DB

refresh() → get updated data



---

📌 3. Database Sessions & Dependency Injection


---

🧠 Why Sessions?

Session = connection to DB
Used to:

Execute queries

Manage transactions



---

🔄 Create Dependency

from app.database import SessionLocal
from fastapi import Depends

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()


---

💡 Explanation

yield → setup + cleanup

Ensures DB connection is closed



---

🔗 Use in Routes

from fastapi import FastAPI, Depends
from sqlalchemy.orm import Session

app = FastAPI()

@app.get("/users/{user_id}")
def read_user(user_id: int, db: Session = Depends(get_db)):
    return db.query(User).filter(User.id == user_id).first()


---

🔥 Transaction Handling

try:
    db.add(user)
    db.commit()
except:
    db.rollback()
    raise


---

💡 Explanation

commit() → save

rollback() → undo if error



---

📌 4. Migrations & Production DB Setup


---

🧠 What are Migrations?

Migrations track database schema changes.

👉 Example:

Add column

Modify table

Create new table



---

📦 Initialize Alembic

alembic init alembic


---

⚙️ Configure Alembic

Edit alembic.ini:

sqlalchemy.url = postgresql://user:password@localhost/dbname


---

🔄 Link Models

In alembic/env.py:

from app.database import Base
from app.models import user

target_metadata = Base.metadata


---

🧱 Create Migration

alembic revision --autogenerate -m "create users table"


---

🚀 Apply Migration

alembic upgrade head


---

💡 Explanation

revision → generates migration script

upgrade → applies changes



---

📌 Production Database Setup


---

🔐 Use Environment Variables

import os

DATABASE_URL = os.getenv("DATABASE_URL")


---

🐳 Docker Setup (Basic)

version: '3.8'

services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: dbname
    ports:
      - "5432:5432"


---

⚡ Connection Pooling

engine = create_engine(DATABASE_URL, pool_size=10, max_overflow=20)


---

🔒 Production Best Practices

Use PostgreSQL (not SQLite)

Enable connection pooling

Use migrations (Alembic)

Secure DB credentials

Backup database regularly



---

🔥 Full Example (End-to-End)

@app.post("/users/")
def create(user: UserSchema, db: Session = Depends(get_db)):
    return create_user(db, user.name, user.email)


---

🔥 Common Mistakes

Not closing DB sessions

Skipping migrations

Using SQLite in production

Not handling transactions



---

🧠 Best Practices

Separate models, schemas, CRUD

Use dependency injection for DB

Use Alembic for migrations

Use PostgreSQL in production

Handle errors with rollback



---

🏁 Summary

Feature	Purpose

SQLAlchemy	ORM for DB interaction
CRUD	Data operations
Sessions	DB connection handling
Alembic	Schema migrations



---

🚀 Next Phase

Middleware & Exception Handling

Logging & Monitoring

Async DB (Advanced)

Deployment (Docker + CI/CD)



---

---

If you want to go **next level (very important for your Tech Lead goal)**, I can create:

✅ **PHASE 6 — Middleware, Logging, Exception Handling (Production)**  
✅ **Async SQLAlchemy (2.0) + FastAPI (high performance)**  
✅ **Full SaaS backend (Auth + DB + Payments + Scaling)**  

Just tell me 👍